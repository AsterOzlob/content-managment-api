# 📚 Content Management API на Golang

Этот проект представляет собой **RESTful API-сервер**, реализованный на языке программирования **Golang**, предназначенный для управления контентом — статьями, пользователями, ролями и медиафайлами.  
Проект создан в рамках практики по разработке серверного приложения с использованием современных технологий.

---

## 🛠️ Используемые технологии

| Технология | Назначение |
|------------|------------|
| **Go 1.24** | Язык программирования |
| **Gin** | Веб-фреймворк для маршрутизации и middleware |
| **GORM** | ORM для работы с PostgreSQL |
| **JWT** | Аутентификация и авторизация через токены |
| **PostgreSQL** | Реляционная база данных |
| **Swagger / Gin-Swagger** | Документирование API |
| **Docker + Docker Compose** | Контейнеризация и развёртывание окружения |
| **Rate Limiting, Middleware** | Безопасность и защита от атак |

---

## 🚀 Как запустить проект

### Предварительные требования
- Установленный [Docker](https://www.docker.com/products/docker-desktop)
- Установленный [Docker Compose](https://docs.docker.com/compose/install/)

### Шаги для запуска

1. Склонируй репозиторий:
   ```bash
   git clone https://github.com/yourusername/cms-api.git
   cd cms-api
   ```

2. Создай `.env` файл на основе шаблона:
   ```bash
   cp .env.example .env
   ```
   Измени значения переменных под себя.

3. Запусти проект:
   ```bash
   docker-compose up --build
   ```

4. Открой Swagger UI:
   ```
   http://localhost:8080/docs/index.html
   ```

5. Остановка:
   ```bash
   docker-compose down
   ```

---

## 📁 Файлы проекта

```
cms-api/
├── main.go                  # Точка входа
├── go.mod / go.sum          # Зависимости Go
├── Dockerfile               # Для сборки Docker-образа
├── docker-compose.yml         # Конфигурация сервисов
├── .env.example             # Пример переменных окружения
├── docs/                    # Генерируемая Swagger-документация
├── api/
│   ├── controllers/         # Обработчики эндпоинтов
│   └── routes/              # Маршруты и middleware
├── internal/
│   ├── database/            # Подключение к БД, миграции, сиды
│   ├── logger/              # Логгирование
│   └── scheduler/           # Планировщик удаления истекших токенов
├── pkg/
│   └── appinit/             # Иницилизация приложения
└── ...
```

---

## 🔐 Роли пользователей и доступы

| Роль | Права |
|------|-------|
| **user** | Может читать статьи и оставлять комментарии |
| **author** | Может создавать и управлять своими статьями |
| **moderator** | Может редактировать и удалять любые комментарии |
| **admin** | Полный доступ ко всем функциям и данным |

---

## 🧪 Эндпоинты API (основные)

> Все эндпоинты подробно описаны в Swagger по адресу:  
> `http://localhost:8080/docs/index.html`

### 🔐 Авторизация
- `POST /users/signup` — регистрация
- `POST /users/login` — вход
- `GET /users/:id` — получить пользователя
- `PATCH /users/:id/role` — назначение роли (только для администратора)

### 📄 Управление статьями
- `GET /articles` — список статей
- `GET /articles/:id` — конкретная статья
- `POST /articles` — создание статьи (автор)
- `PUT /articles/:id` — редактирование статьи (автор)
- `DELETE /articles/:id` — удаление статьи (админ или автор)

### 💬 Комментарии
- `POST /articles/:id/comments` — добавить комментарий (пользователь)
- `DELETE /comments/:id` — удалить комментарий (автор или модератор)

### 🖼️ Медиафайлы
- `POST /media/upload` — загрузить файл (автор)
- `GET /media` — получить список файлов
- `GET /media/:id` — получить файлы по ID статьи
- `DELETE /media/:id` — удалить файл (админ или автор)

---

## 🧪 Примеры тестовых данных

При запуске приложения автоматически создаются следующие пользователи и роли:

### 🎭 Роли
```json
{
  "name": "admin",
  "description": "Полный доступ"
}
{
  "name": "author",
  "description": "Создание и управление статьями"
}
{
  "name": "moderator",
  "description": "Удаление и редактирование комментариев"
}
{
  "name": "user",
  "description": "Чтение и комментирование"
}
```

### 👥 Пользователи
| Имя | Пароль | Роль |
|-----|--------|------|
| admin | password | admin |
| john_doe | password | author |
| jane_moderator | password | moderator |
| guest_user | password | user |

---

## 📄 Документация

API документировано с помощью Swagger и доступно по адресу:

```
http://localhost:8080/docs/index.html
```

Примеры запросов и ответов также можно найти там.

---

## 📦 Упаковка в Docker

Проект развёртывается с помощью `docker-compose.yml` и состоит из двух контейнеров:
- `cms-api` — твой REST API
- `cms-db` — PostgreSQL

Все данные сохраняются между перезапусками.

---

## 📌 Безопасность

- Защита от SQL-инъекций через GORM
- Защита от XSS-атак через валидацию и очистку входных данных
- Rate limiting для защиты от DDoS-атак
- JWT-токены с TTL и refresh-токенами

---

## 📂 Хранение медиафайлов

- Загруженные файлы хранятся в папке `./uploads`
- Поддерживаются форматы: `image/jpeg`, `image/png`, `application/pdf`
- Максимальный размер файла: `5 MB`

---

## 📝 Лицензия

MIT License — см. файл `LICENSE`.

---

## 📞 Поддержка

Если у тебя есть вопросы или предложения, пиши на:
📧 tumoshenko204@mail.ru

---

## 🙌 Автор

**Тимошенко Антон**

---

## 🏁 Готово!

Теперь ты можешь использовать этот `README.md` как полноценное описание проекта для сдачи практики. Он содержит:
- Цель и описание
- Технологии
- Инструкцию по запуску
- Список ролей и эндпоинтов
- Примеры пользователей
- Документацию и безопасность

---

Если хочешь, я могу помочь тебе:
- Автоматизировать генерацию `.env` и `docs/`
- Настроить CI/CD через GitHub Actions
- Сделать Makefile
- Добавить инструкцию по тестированию

Просто напиши 😊
